name: df_tooltip

description: "Customizable Flutter tooltip widget that automatically adjusts its position based on available space"

version: 0.0.1

homepage: https://dartforce.dev/
repository: https://github.com/DartForce22/df_tooltip.git
license: MIT

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true